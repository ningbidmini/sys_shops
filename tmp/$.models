<?php
        class models {
          public $ctrl = null;
          use libs;
          use security;
          use config;
          use database;
          use tplerror;
          function __construct($param=array()){
            // $this->ctrl =new session();
            // $this->setdbconfig();
            echo "Load Models";
          }

        }
      ?>